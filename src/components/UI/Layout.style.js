import styled from 'styled-components'

const Header = styled.header`
  padding: 1rem;
  background: #3742fa;
  height: 10vh;
`

const Ul = styled.ul`
  display: flex;
  justify-content: center;
  align-items: center;
`

const Li = styled.li`
  padding: 0.5rem 1rem;
`

const Main = styled.main`
  padding: 1rem;
`

export { Ul, Header, Li, Main }
